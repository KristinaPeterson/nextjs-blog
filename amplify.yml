version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nmp ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
